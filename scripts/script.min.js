const hamburger=document.querySelector(".hamburger"),navLinks=document.querySelector(".nav-links");hamburger.addEventListener("click",()=>{hamburger.classList.toggle("active"),navLinks.classList.toggle("show")}),document.querySelectorAll(".nav-links a").forEach(e=>{e.addEventListener("click",()=>{hamburger.classList.remove("active"),navLinks.classList.remove("show")})});const lightbox=document.getElementById("lightbox"),lightboxImg=lightbox.querySelector("img"),prevBtn=lightbox.querySelector(".prev"),nextBtn=lightbox.querySelector(".next"),images=Array.from(document.querySelectorAll(".lightbox-img"));let currentIndex=0;function showImage(e){currentIndex=e,lightboxImg.src=images[currentIndex].src}images.forEach((e,t)=>{e.addEventListener("click",()=>{showImage(t),lightbox.classList.add("show")})}),prevBtn.addEventListener("click",e=>{e.stopPropagation(),showImage((currentIndex-1+images.length)%images.length)}),nextBtn.addEventListener("click",e=>{e.stopPropagation(),showImage((currentIndex+1)%images.length)}),lightbox.addEventListener("click",()=>{lightbox.classList.remove("show")});let startX=0,startY=0;lightbox.addEventListener("touchstart",e=>{const t=e.touches[0];startX=t.clientX,startY=t.clientY}),lightbox.addEventListener("touchmove",e=>{if(!startX||!startY)return;const t=e.touches[0],o=t.clientX-startX,i=t.clientY-startY;Math.abs(o)>80&&(o>0?showImage((currentIndex-1+images.length)%images.length):showImage((currentIndex+1)%images.length),startX=0,startY=0),Math.abs(i)>100&&(lightbox.classList.remove("show"),startX=0,startY=0)}),lightbox.addEventListener("touchend",()=>{startX=0,startY=0}),document.getElementById("year").textContent=new Date().getFullYear();